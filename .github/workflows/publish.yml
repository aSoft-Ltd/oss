# This file was generated using Kotlin DSL (.github/workflows/publish.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: Build, Cache then Publish
on:
  push:
    branches:
    - main
env:
  ASOFT_MAVEN_PGP_PRIVATE_KEY: ${{ secrets.ASOFT_MAVEN_PGP_PRIVATE_KEY }}
  ASOFT_MAVEN_PGP_PASSWORD: ${{ secrets.ASOFT_MAVEN_PGP_PASSWORD }}
  ASOFT_NEXUS_PASSWORD: ${{ secrets.ASOFT_NEXUS_PASSWORD }}
  ASOFT_NEXUS_USERNAME: ${{ secrets.ASOFT_NEXUS_USERNAME }}
  TARGETING_ALL: true
jobs:
  keep-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: keep
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :keep-api:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :keep-api:build
        build-root-directory: ./keep
    - id: step-4
      name: ./gradlew :keep-browser:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :keep-browser:build
        build-root-directory: ./keep
    - id: step-5
      name: ./gradlew :keep-file:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :keep-file:build
        build-root-directory: ./keep
    - id: step-6
      name: ./gradlew :keep-mock:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :keep-mock:build
        build-root-directory: ./keep
    - id: step-7
      name: ./gradlew :keep-react-native:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :keep-react-native:build
        build-root-directory: ./keep
  koncurrent-later-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: koncurrent
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :koncurrent-later-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-later-core:build
        build-root-directory: ./koncurrent
    - id: step-4
      name: ./gradlew :koncurrent-later-coroutines:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-later-coroutines:build
        build-root-directory: ./koncurrent
    - id: step-5
      name: ./gradlew :koncurrent-later-test:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-later-test:build
        build-root-directory: ./koncurrent
  koncurrent-executors-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: koncurrent
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :koncurrent-executors-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-executors-core:build
        build-root-directory: ./koncurrent
    - id: step-4
      name: ./gradlew :koncurrent-executors-coroutines:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-executors-coroutines:build
        build-root-directory: ./koncurrent
    - id: step-5
      name: ./gradlew :koncurrent-executors-mock:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-executors-mock:build
        build-root-directory: ./koncurrent
  kase-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kase
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kase-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kase-core:build
        build-root-directory: ./kase
  kevlar-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kevlar
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kevlar-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kevlar-core:build
        build-root-directory: ./kevlar
  kollections-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kollections
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kollections-interoperable:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-interoperable:build
        build-root-directory: ./kollections
    - id: step-4
      name: ./gradlew :kollections-atomic:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-atomic:build
        build-root-directory: ./kollections
  kommander-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kommander
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kommander-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-core:build
        build-root-directory: ./kommander
    - id: step-4
      name: ./gradlew :kommander-coroutines:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-coroutines:build
        build-root-directory: ./kommander
  functions-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: functions
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :functions-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :functions-core:build
        build-root-directory: ./functions
  rendezvous:
    runs-on: ubuntu-latest
    needs:
    - keep-builder
    - koncurrent-later-builder
    - koncurrent-executors-builder
    - kase-builder
    - kevlar-builder
    - kollections-builder
    - kommander-builder
    - functions-builder
    steps:
    - id: step-0
      run: echo "all builds completed. Beginning deployment"
  keep-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: keep
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing keep-api, keep-browser, keep-file, keep-mock, keep-react-native
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :keep-api:publishToSonatype :keep-browser:publishToSonatype :keep-file:publishToSonatype :keep-mock:publishToSonatype :keep-react-native:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./keep
  koncurrent-later-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: koncurrent
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing koncurrent-later-core, koncurrent-later-coroutines, koncurrent-later-test
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-later-core:publishToSonatype :koncurrent-later-coroutines:publishToSonatype :koncurrent-later-test:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./koncurrent
  koncurrent-executors-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: koncurrent
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing koncurrent-executors-core, koncurrent-executors-coroutines, koncurrent-executors-mock
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-executors-core:publishToSonatype :koncurrent-executors-coroutines:publishToSonatype :koncurrent-executors-mock:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./koncurrent
  kase-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kase
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kase-core
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kase-core:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./kase
  kevlar-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kevlar
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kevlar-core
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kevlar-core:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./kevlar
  kollections-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kollections
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kollections-interoperable, kollections-atomic
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-interoperable:publishToSonatype :kollections-atomic:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./kollections
  kommander-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kommander
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kommander-core, kommander-coroutines
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-core:publishToSonatype :kommander-coroutines:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./kommander
  functions-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: functions
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing functions-core
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :functions-core:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./functions

