# This file was generated using Kotlin DSL (.github/workflows/publish.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: Build, Cache then Publish
on:
  push:
    branches:
    - main
env:
  ASOFT_MAVEN_PGP_PRIVATE_KEY: ${{ secrets.ASOFT_MAVEN_PGP_PRIVATE_KEY }}
  ASOFT_MAVEN_PGP_PASSWORD: ${{ secrets.ASOFT_MAVEN_PGP_PASSWORD }}
  ASOFT_NEXUS_PASSWORD: ${{ secrets.ASOFT_NEXUS_PASSWORD }}
  ASOFT_NEXUS_USERNAME: ${{ secrets.ASOFT_NEXUS_USERNAME }}
  TARGETING_ALL: true
jobs:
  functions-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: functions
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :functions-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :functions-core:build
        build-root-directory: ./functions
  kommander-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kommander
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kommander-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-core:build
        build-root-directory: ./kommander
    - id: step-4
      name: ./gradlew :kommander-coroutines:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-coroutines:build
        build-root-directory: ./kommander
  kollections-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kollections
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kollections-interoperable:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-interoperable:build
        build-root-directory: ./kollections
    - id: step-4
      name: ./gradlew :kollections-atomic:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-atomic:build
        build-root-directory: ./kollections
  kevlar-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kevlar
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kevlar-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kevlar-core:build
        build-root-directory: ./kevlar
  kase-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kase
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kase-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kase-core:build
        build-root-directory: ./kase
  koncurrent-executors-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: koncurrent
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :koncurrent-executors-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-executors-core:build
        build-root-directory: ./koncurrent
    - id: step-4
      name: ./gradlew :koncurrent-executors-coroutines:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-executors-coroutines:build
        build-root-directory: ./koncurrent
    - id: step-5
      name: ./gradlew :koncurrent-executors-mock:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-executors-mock:build
        build-root-directory: ./koncurrent
  koncurrent-later-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: koncurrent
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :koncurrent-later-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-later-core:build
        build-root-directory: ./koncurrent
    - id: step-4
      name: ./gradlew :koncurrent-later-coroutines:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-later-coroutines:build
        build-root-directory: ./koncurrent
    - id: step-5
      name: ./gradlew :koncurrent-later-test:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :koncurrent-later-test:build
        build-root-directory: ./koncurrent
  rendezvous:
    runs-on: ubuntu-latest
    needs:
    - functions-builder
    - kommander-builder
    - kollections-builder
    - kevlar-builder
    - kase-builder
    - koncurrent-executors-builder
    - koncurrent-later-builder
    steps:
    - id: step-0
      run: echo "all builds completed. Beginning deployment"
  functions-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: functions
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing functions
      uses: gradle/gradle-build-action@v2
      with:
        arguments: publishAllPublicationsToSonatypeRepository closeAndReleaseStagingRepository
        build-root-directory: ./functions
  kommander-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kommander
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kommander
      uses: gradle/gradle-build-action@v2
      with:
        arguments: publishAllPublicationsToSonatypeRepository closeAndReleaseStagingRepository
        build-root-directory: ./kommander
  kollections-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kollections
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kollections
      uses: gradle/gradle-build-action@v2
      with:
        arguments: publishAllPublicationsToSonatypeRepository closeAndReleaseStagingRepository
        build-root-directory: ./kollections
  kevlar-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kevlar
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kevlar
      uses: gradle/gradle-build-action@v2
      with:
        arguments: publishAllPublicationsToSonatypeRepository closeAndReleaseStagingRepository
        build-root-directory: ./kevlar
  kase-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kase
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kase
      uses: gradle/gradle-build-action@v2
      with:
        arguments: publishAllPublicationsToSonatypeRepository closeAndReleaseStagingRepository
        build-root-directory: ./kase
  koncurrent-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: koncurrent
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing koncurrent
      uses: gradle/gradle-build-action@v2
      with:
        arguments: publishAllPublicationsToSonatypeRepository closeAndReleaseStagingRepository
        build-root-directory: ./koncurrent

