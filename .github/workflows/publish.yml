# This file was generated using Kotlin DSL (.github/workflows/publish.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: Build, Cache then Publish
on:
  push:
    branches:
    - main
env:
  ASOFT_MAVEN_PGP_PRIVATE_KEY: ${{ secrets.ASOFT_MAVEN_PGP_PRIVATE_KEY }}
  ASOFT_MAVEN_PGP_PASSWORD: ${{ secrets.ASOFT_MAVEN_PGP_PASSWORD }}
  ASOFT_NEXUS_PASSWORD: ${{ secrets.ASOFT_NEXUS_PASSWORD }}
  ASOFT_NEXUS_USERNAME: ${{ secrets.ASOFT_NEXUS_USERNAME }}
  TARGETING_ALL: true
jobs:
  functions-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: functions
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :functions-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :functions-core:build
        build-root-directory: ./functions
  kommander-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kommander
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kommander-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-core:build
        build-root-directory: ./kommander
    - id: step-4
      name: ./gradlew :kommander-coroutines:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-coroutines:build
        build-root-directory: ./kommander
  kollections-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kollections
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kollections-api:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-api:build
        build-root-directory: ./kollections
    - id: step-4
      name: ./gradlew :kollections-interoperable:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-interoperable:build
        build-root-directory: ./kollections
    - id: step-5
      name: ./gradlew :kollections-atomic:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-atomic:build
        build-root-directory: ./kollections
  kevlar-builder:
    runs-on: macos-latest
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kevlar
      run: chmod +x ./gradlew
    - id: step-3
      name: ./gradlew :kevlar-core:build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kevlar-core:build
        build-root-directory: ./kevlar
  rendezvous:
    runs-on: ubuntu-latest
    needs:
    - functions-builder
    - kommander-builder
    - kollections-builder
    - kevlar-builder
    steps:
    - id: step-0
      run: echo "all builds completed. Beginning deployment"
  functions-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: functions
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing functions-core
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :functions-core:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./functions
  kommander-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kommander
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kommander-core, kommander-coroutines
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kommander-core:publishToSonatype :kommander-coroutines:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./kommander
  kollections-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kollections
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kollections-api, kollections-interoperable, kollections-atomic
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kollections-api:publishToSonatype :kollections-interoperable:publishToSonatype :kollections-atomic:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./kollections
  kevlar-publisher:
    runs-on: macos-latest
    needs:
    - rendezvous
    steps:
    - id: step-0
      uses: actions/checkout@v3
      with:
        submodules: true
    - id: step-1
      uses: actions/setup-java@v3
      with:
        java-version: 18
        distribution: corretto
    - id: step-2
      name: Make ./gradlew executable
      working-directory: kevlar
      run: chmod +x ./gradlew
    - id: step-3
      name: publishing kevlar-core
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :kevlar-core:publishToSonatype closeAndReleaseStagingRepository
        build-root-directory: ./kevlar

